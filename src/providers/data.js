import types from './type';

const data = [{
    type: types.IMAGE,
    src: 'https://www.pikpng.com/pngl/m/140-1409929_red-butterfly-png-image-background-transparent-background-png.png',
    x: 0,
    y: 0,
    w: -1, // if this is -1, use original width
    h: -1, // if this is -1, use original height
}];

export default data;